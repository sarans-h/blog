<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <fieldset>
        <legend>Form for editting blog</legend>
    <form action="/blogs/<%=foundComment.id%>?_method=PATCH" method="POST">

        <label for="comment">Comment:</label>
        <input type="text" id="comment" name="comment" value="<%=foundComment.comment %>" ><br><br>
        <button type="submit">edit Comment</button>



    </form></fieldset>
</body>
</html>




 -->






<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        html {
  height: 100%;
}
body {
  margin:0;
  padding:0;
  font-family: sans-serif;
  background: rgb(53, 47, 47);
}

.login-box {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 400px;
  padding: 40px;
  transform: translate(-50%, -50%);
  background: rgba(0,0,0,.5);
  box-sizing: border-box;
  box-shadow: 0 15px 25px rgba(0,0,0,.6);
  border-radius: 10px;
}

.login-box h2 {
  margin: 0 0 30px;
  padding: 0;
  color: #fff;
  text-align: center;
}

.login-box .user-box {
  position: relative;
}

.login-box .user-box input {
  width: 100%;
  padding: 10px 0;
  font-size: 16px;
  color: #fff;
  margin-bottom: 30px;
  border: none;
  border-bottom: 1px solid #fff;
  outline: none;
  background: transparent;
}
.login-box .user-box label {
  position: absolute;
  top:0;
  left: 0;
  padding: 10px 0;
  font-size: 16px;
  color: #fff;
  pointer-events: none;
  transition: .5s;
}

.login-box .user-box input:focus ~ label,
.login-box .user-box input:valid ~ label {
  top: -20px;
  left: 0;
  color: rgb(53, 47, 47);
  font-size: 12px;
}

.login-box form a {
  position: relative;
  display: inline-block;
  padding: 10px 20px;
  color: #ffffff;
  font-size: 16px;
  text-decoration: none;
  text-transform: uppercase;
  overflow: hidden;
  transition: .5s;
  margin-top: 40px;
  letter-spacing: 4px
}



.login-box a span {
  position: absolute;
  display: block;
}

.login-box a span:nth-child(1) {
  top: 0;
  left: -100%;
  width: 100%;
  height: 2px;
  background: linear-gradient(90deg, transparent, rgb(53, 47, 47));
  animation: btn-anim1 1s linear infinite;
}



.login-box a span:nth-child(2) {
  top: -100%;
  right: 0;
  width: 2px;
  height: 100%;
  background: linear-gradient(180deg, transparent, rgb(53, 47, 47));
  animation: btn-anim2 1s linear infinite;
  animation-delay: .25s
}



.login-box a span:nth-child(3) {
  bottom: 0;
  right: -100%;
  width: 100%;
  height: 2px;
  background: linear-gradient(270deg, transparent, rgb(53, 47, 47));
  animation: btn-anim3 1s linear infinite;
  animation-delay: .5s
}



.login-box a span:nth-child(4) {
  bottom: -100%;
  left: 0;
  width: 2px;
  height: 100%;
  background: linear-gradient(360deg, transparent, rgb(53, 47, 47));
  animation: btn-anim4 1s linear infinite;
  animation-delay: .75s
}

    </style>
    <title>Edit Blog</title>
</head>
<body>
    <div class="login-box">
        <h2>Editing the blog</h2>
        <form action="/blogs/<%=foundComment.id%>?_method=PATCH" method="POST">
          
          <div class="user-box">
            <textarea id="comment" name="comment" value="" cols="37" rows="5" style="background-color: rgb(53, 47, 47);; border-radius: 12px;padding: 15px;color: #fff;" placeholder="Write Here...."><%=foundComment.comment %></textarea>

          </div>
          <button type="submit" style="background: none;
          border: none;
          font: inherit;
          background-color: #fff;
          margin-top: 22px;
          cursor: pointer;
          outline: inherit;
          color: black;
          width: 80px;
          height: 40px;
          border-radius: 12px;
          ">
         
            
            EDIT
          </button>
        </form>
      </div>
</body>
</html>